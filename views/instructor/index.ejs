<!DOCTYPE html>
<html lang="en">
    <head>
        <title>Instructor | Socialites Excellent Driving</title>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta http-equiv="X-UA-Compatible" content="ie=edge">
        <link rel="icon" type="image/png" sizes="96x96" href="assets/images/icon.png">
        <!-- CSS imports -->
        <% include ../utils/instructorCSS %>
    </head>

    <body class="fix-header fix-sidebar" data-instID='<%=instID%>'>
        <!-- Preloader - style you can find in spinners.css -->
        <div class="preloader">
            <svg class="circular" viewBox="25 25 50 50">
            <circle class="path" cx="50" cy="50" r="20" fill="none" stroke-width="2" stroke-miterlimit="10" /> 
            </svg>
        </div>

        <!-- WRAPPER -->
        <div id="wrapper">
            <% include ../admin/partials/header %> <!-- HEADER CODES -->
            <% include ../admin/partials/sidebar %> <!-- SIDEBAR CODES -->
            <% include ./modals %> 

            <!-- MAIN -->
            <div class="page-wrapper">
                <% include ./main-partials/instSchedule %>
                <% include ./main-partials/instStudent %>
                <% include ./main-partials/instEvaluation %>
                <% include ./main-partials/instAccount %>

                <% include ./minor-partials/addGrades %>
                <% include ./minor-partials/todaySched %>
                <% include ./minor-partials/tomSched %>
            </div>
            <!-- END MAIN -->
            <% include ../admin/partials/footer %> 
            <!-- FOOTER CODES -->

            <!-- JS imports -->
            <% include ../utils/instructorJS %>
        </div>
    </body>
</html>